# Первый запуск (создаются тома)
docker-compose up -d

# Остановка контейнеров (тома остаются)
docker-compose down

# Повторный запуск – данные сохраняются
docker-compose up -d

# Удалить тома вместе с контейнерами (очистить данные)
docker-compose down -v